# modules/linux/desktop.nix — Hyprland + Quickshell (future linux)
# Not evaluated on darwin — same omanix.theme/widgets work via lib/mkSystem branching
{ ... }: {
  # TODO: Phase 08+ — Linux desktop when nixosSystem branch is active
  # services.hyprland.enable = true;
  # services.quickshell.enable = true;
}
