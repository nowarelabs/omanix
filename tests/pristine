#!/bin/bash
# tests/pristine — full pristine uninstall test
# switch → add → remove → uninstall → assert clean
set -e

echo "=== tests/pristine ==="
echo "This test requires sudo and a working nix-darwin setup."
echo "Run on a test Mac or VM, not your daily driver."
echo ""
echo "Test sequence:"
echo "  1. darwin-rebuild switch (current config)"
echo "  2. omanix add ripgrep (nixpkgs)"
echo "  3. darwin-rebuild switch"
echo "  4. which ripgrep (should exist)"
echo "  5. omanix remove ripgrep"
echo "  6. darwin-rebuild switch"
echo "  7. ! which ripgrep (should not exist)"
echo "  8. darwin-rebuild --rollback (back to step 1)"
echo ""
echo "Full pristine test requires:"
echo "  - /nix/nix-installer uninstall"
echo "  - rm -rf ~/.config/omanix"
echo "  - assert no ~/Library/LaunchAgents/org.omanix.*"
echo "  - assert no /Applications/Omanix*"
echo ""
echo "=== tests/pristine (dry run) ==="
echo "Skipped — run manually on test Mac"
